import styled from "styled-components";

const StyledLogo = styled.div`
  width: fit-content;
  display: flex;
  align-items: center;
  cursor: pointer;
  & img {
    height: 25px;
    margin-right: 3px;
  }
`;

export default StyledLogo;
