import styled from "styled-components";

const StyledLayout = styled.div`
  display: grid;
  position: relative;
  grid-template-rows: 54px repeat(100, auto);
`;

export default StyledLayout;
